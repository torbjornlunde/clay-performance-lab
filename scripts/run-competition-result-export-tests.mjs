import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import ts from "typescript";
import vm from "node:vm";

const root = process.cwd();
function load(file, imports = {}) {
  const source = fs.readFileSync(path.join(root, file), "utf8");
  const js = ts.transpileModule(source, { compilerOptions: { module: ts.ModuleKind.CommonJS, target: ts.ScriptTarget.ES2022 } }).outputText;
  const module = { exports: {} }; vm.runInNewContext(`(function(require,module,exports){${js}\n})(require,module,module.exports)`, { module, require: (id) => imports[id] || (() => { throw new Error(`Unexpected import ${id}`); })(), console }); return module.exports;
}
const core = load("lib/scoreSheets/core.ts");
const results = load("lib/scoreSheets/results.ts", { "./core": core });
const csv = load("lib/scoreSheets/resultCsv.ts");
const disciplines = load("lib/disciplines.ts");
const setup = { postCount: 2, targetsPerPost: 10, expectedTargetsByPost: [12, 8] };
const projected = results.projectScoreSheetResults({ setup, shooters: [{ id: "a", shooter_name: "A", display_order: 2 }, { id: "b", shooter_name: "B", display_order: 1 }], scores: [{ shooter_id: "a", post_number: 1, score: 10 }, { shooter_id: "a", post_number: 2, score: 8 }, { shooter_id: "b", post_number: 1, score: 10 }, { shooter_id: "b", post_number: 2, score: 8 }], targetResults: [] });
assert.equal(projected[0].displayName, "B"); assert.equal(projected[0].totalScore, 18); assert.equal(projected[0].expectedTargets, 20); assert.equal(projected[0].scoredTargets, 0); assert.equal(projected[0].unscoredTargets, 20); assert.equal(projected[0].tiedOnTotal, true); assert.equal(projected[1].tiedOnTotal, true);
const mixed = results.projectScoreSheetResults({ setup, shooters: [{ id: "a", shooter_name: "Åse", display_order: 1 }], scores: [{ shooter_id: "a", post_number: 1, score: 10 }, { shooter_id: "a", post_number: 2, score: 8 }], targetResults: Array.from({ length: 12 }, (_, i) => ({ shooter_id: "a", post_number: 1, target_number: i + 1, result: i < 9 ? "hit" : "miss" })) });
assert.deepEqual(Array.from(mixed[0].postScores), [9, 8]); assert.equal(mixed[0].totalScore, 17); assert.equal(mixed[0].scoredTargets, 12); assert.equal(mixed[0].unscoredTargets, 8); assert.equal(mixed[0].tiedOnTotal, false);
assert.equal(results.isAuthoritativeCompetitionResult("competition", "finalized"), true); for (const pair of [["training", "finalized"], ["shared_training", "finalized"], ["competition", "live"]]) assert.equal(results.isAuthoritativeCompetitionResult(...pair), false);
assert.equal(results.isCurrentAuthoritativeResultRevision("T1", { session_type: "competition", competition_status: "finalized", updated_at: "T1" }), true);
assert.equal(results.isCurrentAuthoritativeResultRevision("T1", { session_type: "competition", competition_status: "live", updated_at: "T2" }), false);
assert.equal(results.isCurrentAuthoritativeResultRevision("T1", { session_type: "competition", competition_status: "finalized", updated_at: "T3" }), false);
assert.equal(results.isCurrentAuthoritativeResultRevision("T1", { session_type: "training", competition_status: "finalized", updated_at: "T1" }), false);
assert.equal(results.isCurrentAuthoritativeResultRevision("T1", { session_type: "shared_training", competition_status: "finalized", updated_at: "T1" }), false);
const twentyPostScores = Array.from({ length: 20 }, (_, index) => ({ shooter_id: "wide", post_number: index + 1, score: index + 1 }));
const twentyPostResult = results.projectScoreSheetResults({ setup: { postCount: 20, targetsPerPost: 25 }, shooters: [{ id: "wide", shooter_name: "Twenty post shooter", display_order: 1 }], scores: twentyPostScores, targetResults: [] });
assert.equal(twentyPostResult[0].postScores.length, 20); assert.deepEqual(Array.from(twentyPostResult[0].postScores), Array.from({ length: 20 }, (_, index) => index + 1));
const output = csv.buildCompetitionResultCsv({ competition: "=Cup, North", date: "2026-08-09", location: "Møre\nNorway", discipline: "Compak", finalizedAt: "2026-08-09T12:00:00.000Z", reopenCount: 1, finalizedIncomplete: true, postLabel: "Plate" }, mixed);
assert.ok(output.startsWith("\uFEFFCompetition,Date,Location,Discipline,Finalized at,Corrected,Reopen count,Finalized incomplete,Shooter,Plate 1,Plate 2,Total,Scored targets,Unscored targets"));
for (const [discipline, expectedLabel] of [["Compak Sporting", "Plate"], ["Leirduesti", "Post"], ["Sporting", "Stand"], ["Local Pool Shoot", "Stand"]]) { const label = disciplines.getDisciplineDefinition(discipline).areaSingular; assert.equal(label, expectedLabel); const disciplineCsv = csv.buildCompetitionResultCsv({ competition: "Cup", date: "2026-08-09", location: "", discipline, finalizedAt: "2026-08-09T12:00:00.000Z", reopenCount: 0, finalizedIncomplete: false, postLabel: label }, mixed); assert.ok(disciplineCsv.split("\n", 1)[0].includes(`Shooter,${expectedLabel} 1,${expectedLabel} 2,Total`), `${discipline} CSV uses shared terminology`); } assert.match(output, /"'=Cup, North"/); assert.match(output, /"Møre\nNorway"/); assert.match(output, /Åse/); assert.match(output, /,9,8,17,12,8/); assert.match(output, /,Yes,1,Yes,/);
assert.equal(csv.escapeCsvCell('Simon "The Shooter", Jr.', true), '"Simon ""The Shooter"", Jr."'); assert.equal(csv.sanitizeCsvText("@SUM(A1:A2)"), "'@SUM(A1:A2)"); assert.equal(csv.sanitizeCsvText(" +CMD"), "' +CMD"); assert.equal(csv.escapeCsvCell(42), "42"); assert.equal(csv.competitionResultFilename("2026-08-09"), "competition-results-2026-08-09.csv"); assert.equal(csv.competitionResultFilename("../../bad"), "competition-results-result.csv");
const page = fs.readFileSync(path.join(root, "app/competition-score-sheets/[id]/result/page.tsx"), "utf8"); const css = fs.readFileSync(path.join(root, "app/globals.css"), "utf8"); const training = fs.readFileSync(path.join(root, "app/training-score-sheets/[id]/page.tsx"), "utf8");
assert.match(page, /competition_reopen_count,updated_at/); assert.match(page, /async function validateCurrentFinalizedRevision/); assert.match(page, /\.eq\("competition_status", "finalized"\)[\s\S]*\.eq\("updated_at", loadedRevision\)/);
const childLoadIndex = page.indexOf("Promise.all"); const loadRevalidationIndex = page.indexOf("validateCurrentFinalizedRevision(loaded.id", childLoadIndex); const readyIndex = page.indexOf('setState("ready")', loadRevalidationIndex); assert.ok(childLoadIndex >= 0 && loadRevalidationIndex > childLoadIndex && readyIndex > loadRevalidationIndex, "ready follows child-load finalized revision revalidation");
const printAction = page.slice(page.indexOf("async function printResult"), page.indexOf("async function downloadCsv")); assert.match(printAction, /await resultRevisionIsStillCurrent\("printing"\)[\s\S]*window\.print\(\)/); assert.equal(printAction.indexOf("resultRevisionIsStillCurrent" ) < printAction.indexOf("window.print()"), true);
const downloadAction = page.slice(page.indexOf("async function downloadCsv"), page.indexOf('if (state === "loading")')); assert.match(downloadAction, /await resultRevisionIsStillCurrent\("downloading"\)/); assert.ok(downloadAction.indexOf("resultRevisionIsStillCurrent") < downloadAction.indexOf("new Blob"), "CSV validation precedes Blob creation"); assert.match(page, /URL\.revokeObjectURL/);
assert.match(page, /getDisciplineDefinition\(sheet\?\.discipline\)\.areaSingular/); assert.match(page, /postLabel: areaLabel/); assert.doesNotMatch(page, /includes\("compak"\)/);
assert.match(page, /competitionResultTableWrap/); assert.match(page, /competitionResultPrint[\s\S]*row\.postScores\.map/); assert.match(css, /\.competitionResultPrint \{ display: none; \}/); assert.match(css, /@media print[\s\S]*\.competitionResultTableWrap \{ display: none[\s\S]*\.competitionResultPrint \{ display: block/); assert.match(css, /grid-template-columns: repeat\(auto-fit/);
assert.doesNotMatch(training, /Print result|Download CSV|View final result/); assert.equal(fs.existsSync(path.join(root, "lib/scoreSheets/competitionScoring.ts")), false); assert.doesNotMatch(page, /from\("sessions"\)|localStorage|public.*result|service.role/i);
const stats = fs.readFileSync(path.join(root, "app/stats/page.tsx"), "utf8"); assert.match(stats, /\.in\("session_type", \["training", "shared_training"\]\)/);
console.log("Competition result projection, CSV safety, private route, print wiring, and separation checks passed.");
