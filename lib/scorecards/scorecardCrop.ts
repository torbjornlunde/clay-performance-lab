export type NormalizedCrop = { x: number; y: number; width: number; height: number; mode?: "full" | "crop" };
export function clampCrop(crop: NormalizedCrop): NormalizedCrop { const x=Math.max(0,Math.min(1,crop.x)); const y=Math.max(0,Math.min(1,crop.y)); const width=Math.max(0.02,Math.min(1-x,crop.width)); const height=Math.max(0.02,Math.min(1-y,crop.height)); return { x, y, width, height, mode: crop.mode || (x===0&&y===0&&width===1&&height===1?"full":"crop") }; }
export const fullImageCrop: NormalizedCrop = { x: 0, y: 0, width: 1, height: 1, mode: "full" };
export function isFullImageCrop(crop?: NormalizedCrop | null) { if(!crop) return true; const c=clampCrop(crop); return c.x===0&&c.y===0&&c.width===1&&c.height===1; }
export async function renderCropBlob(image: Blob, crop: NormalizedCrop): Promise<Blob> { const bmp = await createImageBitmap(image); const c=clampCrop(crop); const sx=Math.round(c.x*bmp.width), sy=Math.round(c.y*bmp.height), sw=Math.max(1,Math.round(c.width*bmp.width)), sh=Math.max(1,Math.round(c.height*bmp.height)); const canvas=document.createElement("canvas"); canvas.width=Math.min(2200,sw); canvas.height=Math.max(1,Math.round(sh*(canvas.width/sw))); const ctx=canvas.getContext("2d"); if(!ctx) throw new Error("Could not prepare crop."); ctx.drawImage(bmp,sx,sy,sw,sh,0,0,canvas.width,canvas.height); const blob=await new Promise<Blob|null>((res)=>canvas.toBlob(res,"image/jpeg",0.84)); if(!blob) throw new Error("Could not prepare crop."); return blob; }
export async function fingerprintCrop(blob: Blob, crop: NormalizedCrop) { const hash=await crypto.subtle.digest("SHA-256", await blob.arrayBuffer()); const base=Array.from(new Uint8Array(hash)).map((b)=>b.toString(16).padStart(2,"0")).join(""); return base; }
export function sameCrop(a?: NormalizedCrop|null,b?: NormalizedCrop|null){ const ca=clampCrop(a||fullImageCrop), cb=clampCrop(b||fullImageCrop); return ["x","y","width","height"].every((k)=>Math.abs((ca as any)[k]-(cb as any)[k])<0.0001); }
