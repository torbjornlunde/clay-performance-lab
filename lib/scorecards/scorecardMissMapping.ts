import type { ScorecardCell } from "./scorecardAnalysis";
export type PostTargetRow = { post_number:number; target_position:number; presentation_number:number|null; presentation_type:string|null; position_in_presentation:number|null; target_label:string|null; target_type:string|null };
export type ExistingMiss = { course_number:number|null; target_position?:number|null; target_number:number|null; missed_target:string|null; source_type?:string|null };
export type ImportMissRow = { course_number:number; target_position:number; target_number:number; target_label:string; target_type:string; base_presentation:string; actual_presentation:string; missed_target:string; where_miss:string; main_reason:string; target_read:string; comment:null };
function atom(post:number,pos:number){return `${post}:${pos}`;}
export function existingMissAtoms(misses: ExistingMiss[]) { const atoms = new Set<string>(); let ambiguous = false; for (const m of misses) { const post = m.course_number; if (!post) { ambiguous = true; continue; } if (m.source_type === "scorecard_import" && m.target_position) { atoms.add(atom(post,m.target_position)); continue; } if (m.target_position) { atoms.add(atom(post,m.target_position)); continue; } const tn = m.target_number; if (!tn) { ambiguous = true; continue; } const missed = (m.missed_target || "").toLowerCase(); if (missed.includes("both")) { atoms.add(atom(post,tn*2-1)); atoms.add(atom(post,tn*2)); } else if (missed.includes("first")) atoms.add(atom(post,tn*2-1)); else if (missed.includes("second")) atoms.add(atom(post,tn*2)); else if (missed.includes("single")) atoms.add(atom(post,tn)); else ambiguous = true; } return { atoms, ambiguous }; }
export function mapReviewedMisses(grid: ScorecardCell[], targets: PostTargetRow[], existing: ExistingMiss[]) { const targetMap = new Map(targets.map(t=>[atom(t.post_number,t.target_position),t])); const existingAtoms = existingMissAtoms(existing); const rows: ImportMissRow[] = []; let skippedDuplicates = 0; for (const cell of grid.filter(c=>c.result==="miss")) { const key = atom(cell.postNumber, cell.targetNumber); if (existingAtoms.atoms.has(key)) { skippedDuplicates++; continue; } const def = targetMap.get(key); const pairPos = def?.position_in_presentation || null; const presentation = def?.presentation_type || "Unknown"; rows.push({ course_number: cell.postNumber, target_position: cell.targetNumber, target_number: def?.presentation_number || cell.targetNumber, target_label: def?.target_label || `Post ${cell.postNumber} · Target ${cell.targetNumber}`, target_type: def?.target_type || "Unknown", base_presentation: presentation, actual_presentation: presentation, missed_target: !def ? "Unknown" : presentation === "single" ? "Single target" : pairPos === 1 ? "First target in pair" : pairPos === 2 ? "Second target in pair" : "Unknown", where_miss: "Not sure", main_reason: "Unknown", target_read: "Unknown", comment: null }); } return { rows, skippedDuplicates, ambiguousExisting: existingAtoms.ambiguous, score: grid.filter(c=>c.result==="hit").length, totalTargets: grid.length, misses: grid.filter(c=>c.result==="miss").length }; }
