"use client";import {useMemo,useState} from "react";import {useRouter} from "next/navigation";import {supabase} from "@/lib/supabase/client";import {defaultStartPlateForShooter,getSchemeOptions,plateRotation} from "@/lib/fitasc/schemes";
type C={courseNumber:number;scheme:number;shooterNumber:number;startPlate:number};function mk(n:number,old:C[]){return Array.from({length:n},(_,i)=>old[i]?{...old[i],courseNumber:i+1}:{courseNumber:i+1,scheme:i+1,shooterNumber:1,startPlate:1});}
export default function NewSessionPage(){const router=useRouter();const schemes=useMemo(()=>getSchemeOptions(),[]);const[name,setName]=useState("");const[discipline,setDiscipline]=useState("Compak Sporting");const[sessionType,setSessionType]=useState("Training");const[format,setFormat]=useState("Inline");const[count,setCount]=useState(3);const[courses,setCourses]=useState<C[]>(mk(3,[]));const[leirdueResultUrl,setLeirdueResultUrl]=useState("");const[err,setErr]=useState("");const[saving,setSaving]=useState(false);function setCourseCount(n:number){setCount(n);setCourses(c=>mk(n,c));}function upd(i:number,u:Partial<C>){setCourses(c=>c.map((x,idx)=>idx===i?{...x,...u}:x));}async function save(){setErr("");setSaving(true);const{data:u}=await supabase.auth.getUser();if(!u.user){router.push("/login");return;}const isCompak=discipline==="Compak Sporting";const{data:s,error}=await supabase.from("sessions").insert({user_id:u.user.id,name:name.trim()||"Unnamed session",discipline,session_type:sessionType,shooting_format:isCompak?format:null,course_count:isCompak?count:null,total_targets:isCompak?count*25:null,leirdue_result_url:leirdueResultUrl.trim()||null}).select("id").single();if(error||!s){setErr(error?.message||"Could not save");setSaving(false);return;}if(isCompak){const rows=courses.map(c=>({session_id:s.id,course_number:c.courseNumber,fitasc_scheme:c.scheme,shooter_number:format==="Squad"?c.shooterNumber:null,start_plate:format==="Squad"?c.startPlate:null}));const{error:e2}=await supabase.from("session_courses").insert(rows);if(e2){setErr(e2.message);setSaving(false);return;}}router.push(`/sessions/${s.id}`);}return <main><div className="card"><h2>New session</h2><label>Session name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Session name"/><label>Leirdue.net result URL</label><input value={leirdueResultUrl} onChange={e=>setLeirdueResultUrl(e.target.value)} placeholder="https://www.leirdue.net/..." type="url"/><div className="row"><div><label>Discipline</label><select value={discipline} onChange={e=>setDiscipline(e.target.value)}><option>Compak Sporting</option><option>Sporting</option><option>FITASC Sporting</option><option>Leirduesti</option><option>Trap</option><option>Skeet</option><option>Other</option></select></div><div><label>Session type</label><select value={sessionType} onChange={e=>setSessionType(e.target.value)}><option>Training</option><option>Competition</option></select></div></div>{discipline==="Compak Sporting"&&<><div className="row"><div><label>Number of courses/layouts</label><select value={count} onChange={e=>setCourseCount(Number(e.target.value))}>{[1,2,3,4,5,6,8].map(n=><option key={n} value={n}>{n}</option>)}</select></div><div><label>Shooting format</label><select value={format} onChange={e=>setFormat(e.target.value)}><option>Inline</option><option>Squad</option></select></div></div><h3>Courses</h3>{courses.map((c,i)=><div className="subcard" key={c.courseNumber}><h3>Course {c.courseNumber}</h3><label>FITASC scheme</label><select value={c.scheme} onChange={e=>upd(i,{scheme:Number(e.target.value)})}>{schemes.map(o=><option key={o.scheme} value={o.scheme}>{o.label}</option>)}</select>{format==="Squad"&&<><div className="row"><div><label>Shooter number</label><select value={c.shooterNumber} onChange={e=>{const shooterNumber=Number(e.target.value);upd(i,{shooterNumber,startPlate:defaultStartPlateForShooter(shooterNumber)})}}>{[1,2,3,4,5,6].map(n=><option key={n}>{n}</option>)}</select></div><div><label>Starting plate</label><select value={c.startPlate} onChange={e=>upd(i,{startPlate:Number(e.target.value)})}>{[1,2,3,4,5].map(n=><option key={n}>{n}</option>)}</select></div></div><p className="small muted">Rotation: {plateRotation(c.startPlate).join(" → ")}</p></>}</div>)}</>}{err&&<div className="error">{err}</div>}<div className="btns"><button onClick={save} disabled={saving}>{saving?"Saving...":"Save session"}</button><button className="secondary" onClick={()=>router.push("/dashboard")}>Cancel</button></div></div></main>}
