"use client";import {useEffect,useMemo,useState} from "react";import Link from "next/link";import {useParams,useRouter} from "next/navigation";import {supabase} from "@/lib/supabase/client";import {getTargetTypeForScheme} from "@/lib/fitasc/schemes";
export default function LogPage(){const params=useParams<{id:string}>();const router=useRouter();const[s,setS]=useState<any>(null);const[courses,setCourses]=useState<any[]>([]);const[courseNumber,setCourseNumber]=useState(1);const[plate,setPlate]=useState(1);const[targetNumber,setTargetNumber]=useState(1);const[missedTarget,setMissedTarget]=useState('Single target');const[whereMiss,setWhereMiss]=useState('Behind');const[mainReason,setMainReason]=useState('Technical');const[targetRead,setTargetRead]=useState('Normal');const[comment,setComment]=useState('');const[msg,setMsg]=useState('');useEffect(()=>{load()},[]);const current=useMemo(()=>courses.find(c=>c.course_number===courseNumber)||courses[0],[courses,courseNumber]);const targetType=current?getTargetTypeForScheme(current.fitasc_scheme||1,targetNumber):'Unknown';useEffect(()=>{setMissedTarget(targetType==='Single'?'Single target':'Second target in pair')},[targetType]);async function load(){const{data:u}=await supabase.auth.getUser();if(!u.user){router.push('/login');return;}const{data:sd}=await supabase.from('sessions').select('id,name,discipline,shooting_format').eq('id',params.id).single();const{data:cd}=await supabase.from('session_courses').select('*').eq('session_id',params.id).order('course_number');setS(sd);setCourses(cd||[]);if(cd?.[0]){setCourseNumber(cd[0].course_number);if(sd?.shooting_format==='Squad'&&cd[0].start_plate)setPlate(cd[0].start_plate)}}function changeCourse(n:number){setCourseNumber(n);const c=courses.find(x=>x.course_number===n);if(s?.shooting_format==='Squad'&&c?.start_plate)setPlate(c.start_plate);setTargetNumber(1)}async function save(){setMsg('');if(!s||!current){setMsg('Session/course missing.');return;}const{error}=await supabase.from('misses').insert({session_id:s.id,course_number:courseNumber,plate,target_number:targetNumber,target_label:`Target ${targetNumber}`,target_type:targetType,missed_target:missedTarget,where_miss:whereMiss,main_reason:mainReason,target_read:targetRead,comment:comment.trim()||null});if(error){setMsg(error.message);return;}setComment('');setMsg('Miss saved.')} if(!s)return <main><div className="card">Loading...</div></main>;return <main><div className="card"><h2>Log miss</h2><p className="small muted">{s.name}</p>{s.discipline==='Compak Sporting'&&<><div className="row"><div><label>Course</label><select value={courseNumber} onChange={e=>changeCourse(Number(e.target.value))}>{courses.map(c=><option key={c.id} value={c.course_number}>Course {c.course_number} — Scheme {c.fitasc_scheme}</option>)}</select></div><div><label>Plate</label><select value={plate} onChange={e=>setPlate(Number(e.target.value))}>{[1,2,3,4,5].map(v=><option key={v} value={v}>Plate {v}</option>)}</select></div></div><label>Target</label><select value={targetNumber} onChange={e=>setTargetNumber(Number(e.target.value))}>{[1,2,3,4,5].map(v=><option key={v} value={v}>Target {v}</option>)}</select><div className="notice small">Detected target type: <strong>{targetType}</strong></div></>}<div className="row"><div><label>Missed target</label><select value={missedTarget} onChange={e=>setMissedTarget(e.target.value)}><option>Single target</option><option>First target in pair</option><option>Second target in pair</option><option>Both targets in pair</option><option>Unknown</option></select></div><div><label>Where was the miss?</label><select value={whereMiss} onChange={e=>setWhereMiss(e.target.value)}><option>Behind</option><option>In front</option><option>Over</option><option>Under</option><option>Not sure</option></select></div></div><div className="row"><div><label>Main reason</label><select value={mainReason} onChange={e=>setMainReason(e.target.value)}><option>Technical</option><option>Tactical</option><option>Mental</option><option>Fatigue</option><option>Target difficulty</option><option>Weather/wind</option><option>Unknown</option></select></div><div><label>Target read</label><select value={targetRead} onChange={e=>setTargetRead(e.target.value)}><option>Normal</option><option>Looked faster than expected</option><option>Looked slower than expected</option><option>Wind affected</option><option>Poor visibility</option><option>Unknown</option></select></div></div><label>Short comment</label><textarea value={comment} onChange={e=>setComment(e.target.value)} />{msg&&<div className={msg.includes('saved')?'success':'error'}>{msg}</div>}<div className="btns"><button onClick={save}>Save miss</button><Link className="button secondary" href={`/sessions/${params.id}`}>Back</Link><Link className="button secondary" href={`/sessions/${params.id}/analysis`}>Analysis</Link></div></div></main>}
